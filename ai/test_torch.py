import sys
print("starting torch", flush=True)
import torch
print("torch imported", flush=True)
print("starting transformers", flush=True)
import transformers
print("transformers imported", flush=True)
